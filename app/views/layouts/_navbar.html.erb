<div class="web-header">
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="margin-bottom-8">
      <a href="/" class="navbar-middle-left"><%= image_tag("logo/logo_light.png", alt: "BôkaBook", height: '40') %></a>
      
      <form class="form-inline pull-xs-right">
        <% if user_signed_in? %>
            <%= link_to "Hello #{current_user.name || current_user.email}", current_user, class: 'navbar-link' %>
            <%= link_to image_tag(current_user.avatar, class: "img-rounded avatar"), current_user %>
            <%= link_to "Transaction", cart_path, class: "btn btn-link" %>
            <%= link_to "Members", users_path, class: 'btn btn-link' %>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-primary" %>
        <% else %>
          <%= link_to "Create new account", new_user_registration_path, class: "btn btn-primary" %> 
          <%= link_to "Sign in", new_user_session_path, class: "btn btn-primary" %>
        <% end %>
      </form>
    </div>
  </nav>
</div>
