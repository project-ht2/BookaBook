<!--if the message from current user, it will render on the right-->
  <li class="media">
    <div class="media-body">
      <h4 class="media-heading text-align-right"><%= message.sender.name %> nhắn</h4>
      <p><%= message.body %></p>  
    </div>
    <div class="media-right">
      <a href="<%= user_path(id: message.sender.id) %>">
        <%= image_tag(message.sender.avatar(40), class: "media-object img-rounded small-avatar") %>
      </a>
    </div>
  </li>