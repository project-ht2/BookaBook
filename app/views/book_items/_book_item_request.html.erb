<% if !book_item.nil? %>
  <!-- Button trigger modal -->
  <%= render partial: "users/user_small_avatar", object: book_item.shelf.user, as: :user, locals: {book_item: book_item} %>
  <%= render partial: "book_items/book_item_request_modal", object: book_item, as: :book_item %>
<% end %>