<% if @transaction.errors.any? %>
    alert("not valid.")
<% else %>
  $("#show_book_item_<%= @transaction.book_item.id %>").html("<%= escape_javascript(render 'books/book_small', book: @transaction.book_item.book) %>")
<% end %>